# Site edits — terminology pass

Transcribed from `For Brian QUANTEC webpage August 2026 edits v2.docx`, an untracked working file
in `C:\Users\BRA008\Downloads\Quantec\`. Word metadata: author **Moiseenko, Vitali**, created
2026-08-28, last modified **2026-08-31**, 4 pages, 168 words, 3 embedded screenshots.

**This document is later than every ruling in [update-requests.md](update-requests.md)** (2026-08-02)
and later than the lung V20 amendment (2026-08-03). Where it conflicts with those, it supersedes
them — see "Reconciliation" below.

## The theme

Every edit in the document moves the site's vocabulary in one direction: away from words that
assert a limit the planner must respect (*constraints*, *dose limits*, *dose thresholds*) and
toward words that describe observed data (*toxicity risk metrics*, *dose-volume data*,
*dose-volume correlates*, *dose metrics*).

This is a clinical-framing change, not a cosmetic one. It aligns the site's language with what
the QUANTEC tables actually are — pooled outcome data with stated caveats — rather than
prescriptive planning limits. No dose value, paper title, author list or URL is altered by any
item below.

## Verbatim transcription

The document is short. Its full text, exactly as written:

> Home page. Replace “constraints” with “toxicity risk metrics”
>
> 1. Change to
>    Our mission is to provide comprehensive, evidence-based toxicity risk metrics and resources to assist radiation oncology professionals worldwide in determining acceptable dose/volume planning objectives
> 2. Change “dose thresholds” to “dose-volume metrics”
> 3. Change “constraints” to “dose-volume correlates”
>
> Under “By organ system”
> CNS, you have Brainstem one word, and then brain stem two words, please pick one
> HN, replace “Cochlear dose thresholds” with “Cochlear dose metrics”
> Cardiovascular System, please replace “Hearth dose constraints” with “Heart dose-volume data”
> Respiratory System, please replace “Lung V20 constraints” with “Lung V20 data”
> GI System, please replace:
> “Small bowel constraints” with “Small bowel dose-volume data”
> “Liver dose limits” with “Liver dose correlates”
> “Gastric dose thresholds” with “Gastric dose metrics”
> Under “About” tab
> https://quantecradiation.org/about/
>
> 1 and 2, replace with “dose/constraint” (1) and “dose constraints” (2) with toxicity risk metrics

The numbered lists are keyed to red annotations drawn on the screenshots, not to the document's
own paragraph order.

## The three screenshots

**image1.png** — the site header as rendered on the home page. Two red circles, unnumbered:

- the word `dose/constraint` inside the tagline "Evidence-based radiation dose/constraint
  resources for radiation oncology"
- the nav tab **Constraints**

**image2.png** — the full home page, three numbered red annotations:

| Mark | Element circled | Current text |
|---|---|---|
| 1 | Hero mission sentence | "Our mission is to provide comprehensive, evidence-based radiation dose constraints and guidelines for radiation oncology professionals worldwide." |
| 2 | "CNS Tolerance" card body | "Central nervous system **dose thresholds** and cognitive function preservation strategies." |
| 3 | "Cardiac Toxicity" card body | "Heart dose **constraints** and radiation-induced cardiac complications." |

These are the referents of numbered items 1, 2 and 3 in the transcription above.

**image3.png** — the About page header and hero, two numbered red annotations:

| Mark | Element circled | Current text |
|---|---|---|
| 1 | Tagline in the site header | "Evidence-based radiation **dose/constraint** resources for radiation oncology" |
| 2 | About hero subtitle | "Advancing radiation oncology through evidence-based **dose constraints** and clinical research." |

These are the referents of the final line. Note that image3's mark 1 and image1's tagline circle
are **the same element** — the tagline is site-wide, so it is one edit, not two.

## Item-by-item mapping

Line numbers are as of commit `04fe9c9`.

| # | Instruction | File | Current text | New text |
|---|---|---|---|---|
| 1 | Tagline (image1 + image3 mark 1) | `_config.yml:2` | `Evidence-based radiation dose/constraint resources for radiation oncology` | `Evidence-based radiation toxicity risk metrics for radiation oncology` |
| 2 | Nav tab (image1) | `_layouts/default.html:32` | `Constraints` | `Toxicity Risk Metrics` |
| 3 | Hero mission (image2 mark 1) | `index.html:9` | "…evidence-based radiation dose constraints and guidelines for radiation oncology professionals worldwide." | the owner's supplied sentence, verbatim |
| 4 | CNS card (image2 mark 2) | `index.html:29` | `Central nervous system dose thresholds and…` | `Central nervous system dose-volume metrics and…` |
| 5 | Cardiac card (image2 mark 3) | `index.html:41` | `Heart dose constraints and radiation-induced…` | `Heart dose-volume correlates and radiation-induced…` |
| 6 | About hero (image3 mark 2) | `about.html:9` | `…evidence-based dose constraints and clinical research.` | `…evidence-based toxicity risk metrics and clinical research.` |
| 7 | CNS spelling | `quantec.html:34,141,143,161` | `Brainstem` / `brain stem` both used | `Brainstem` (one word) throughout prose and labels |
| 8 | Head and neck | `quantec.html:40` | `Cochlear dose thresholds` | `Cochlear dose metrics` |
| 9 | Cardiovascular | `quantec.html:49` | `Heart dose constraints` | `Heart dose-volume data` |
| 10 | Respiratory | `quantec.html:60` | `Lung V20 constraints` | `Lung V20 data` |
| 11 | GI | `quantec.html:71` | `Small bowel constraints` | `Small bowel dose-volume data` |
| 12 | GI | `quantec.html:72` | `Liver dose limits` | `Liver dose correlates` |
| 13 | GI | `quantec.html:74` | `Gastric dose thresholds` | `Gastric dose metrics` |

### Notes on individual items

**Item 1.** The tagline is `site.description` in `_config.yml`, not a literal in the layout. It
is therefore also the site-wide SEO meta description that `{% seo %}` emits on every page, so
this edit changes both the visible tagline and page metadata. `_config.yml` changes are not
picked up by `jekyll serve --watch` — the server must be restarted.

**Item 3.** The owner supplied the replacement sentence in full, so it is transcribed verbatim
rather than composed. Note it drops "and guidelines" and adds a purpose clause ("in determining
acceptable dose/volume planning objectives"). The current sentence wraps `evidence-based
radiation dose constraints` in `<span class="highlight">`; the replacement keeps that span around
the corresponding phrase.

**Item 9.** The document reads "Hearth dose constraints". The site says `Heart dose constraints`
(`quantec.html:49`) and the owner's own replacement text spells it "Heart", so this is a typo in
the quoted string, not a request to change a word that exists. Applied to the real string.

**Item 10.** This bullet's wording was itself an owner amendment on 2026-08-03 (see
[update-requests.md](update-requests.md), ruling 2 supersession). The new wording still names V20
and still does not name V30, so it remains consistent with the verified lung table, which carries
`V20 ≤ 30%` and five mean-dose rows and no V30 row.

**Items 11–13.** These three bullets are link text inside `<a>` elements pointing at
`/publications/#abdomen`. Only the visible text changes; the hrefs are untouched.

## Content-accuracy check

Every item is qualitative framing prose. **No item alters a dose value, a DVH cut-off, an NTCP
parameter, a paper title, an author list, a journal reference or a URL.** Items 8–13 weaken
rather than strengthen the claim each bullet makes (a "limit" becomes a "correlate"), which is
the safe direction of travel for this site.

One string must **not** be swept up by item 7: the paper title
"Radiation Associated Brainstem Injury" at `quantec.html:161` is a citation and is copied from
the source. It keeps the one-word spelling regardless of which spelling the prose adopts.

## Reconciliation with earlier rulings

| Prior decision | Status under this document |
|---|---|
| Ruling 3 (2026-08-02) — nav relabelled "Constraints", URL stays `/quantec/` | **Q1 below.** If the nav is renamed, this document is later and supersedes the label half of ruling 3. The URL half is untouched: `/quantec/` stays either way, so no redirect stub is needed. |
| Lung V20 amendment (2026-08-03) — bullet reads "Lung V20 constraints", do not restore V30 | **Superseded on wording, preserved on substance.** Item 10 changes it to "Lung V20 data". V30 is still absent, which was the point of the amendment. |
| Ruling 2 / 2b (2026-08-02) — organ bullets restored verbatim to the owner's words | **Consistent.** Items 8–13 are the same owner amending their own wording. |
| Ruling 5 (2026-08-02) — ear/cochlea sits under Head and Neck | **Untouched.** Item 8 changes that bullet's wording, not its placement. |

## Rulings — 2026-09-03

All three questions were settled by the owner before implementation. **These are decisions, not
defects — do not re-flag them.**

| # | Question | Ruling |
|---|---|---|
| Q1 | Nav tab rename | **Renamed in full to "Toxicity Risk Metrics", taller header accepted.** The measured cost was far smaller than feared — see below. |
| Q2 | Reach of the terminology sweep | **Sweep everything.** All seven unquoted occurrences changed, plus five more found during implementation. |
| Q3 | Brain stem spelling | **One word, "Brainstem"**, against the recommendation below. Four two-word occurrences changed; the paper title was already one word and is untouched. |

### Q1 — Does the nav tab "Constraints" get renamed, and to what?

image1 circles the nav tab as well as the tagline, and the caption says replace "constraints"
with "toxicity risk metrics". Read literally, the tab becomes **Toxicity Risk Metrics**.

The obstacle is physical. `assets/css/style.css:58-62` records the measurement: the six nav items
occupy 569px of a 594px budget at 1280px — 25px of slack — and the comment warns that at 605px
the nav drops to its own row and the header grows from 130px to 167px. "Constraints" (11
characters) → "Toxicity Risk Metrics" (21) adds roughly 75px. That is far outside the slack, so
the literal rename wraps the header on every page unless something else gives.

**Ruled: rename in full.** The URL stays `/quantec/`, so no redirect is needed and ruling 3's
URL half is untouched; only its label half is superseded.

**The predicted cost did not materialise.** Measured 2026-09-03 in an iframe at exact widths:
the nav grew 569px → 632px as expected, but the header still renders as **one row of 130px at
1280px**, and the single-row threshold moved only **1190px → 1204px**. The reason is that the
tagline shortened in the same pass, narrowing the logo block 526px → 477px and paying back most
of the nav's growth. Below 1204px the header is two rows at 167px, as it was below 1190px before.

On mobile the change is a small *improvement*: at 320px and 360px the header is 291px tall
against 317px before, because the shorter tagline wraps to fewer lines. The nav item itself never
wraps (26px at every width from 320px to 768px). `style.css` and the `content-editing` skill both
carried the old 569px/594px figures and have been updated with these measurements.

### Q2 — How far does the terminology sweep reach?

The document quotes exact strings, and each mapped item above is one of them. But the same
vocabulary appears in seven places the document does not mention, and leaving them makes the
home page say "toxicity risk metrics" in the tagline while a button underneath says "Browse
Constraints":

| File | Line | Text |
|---|---|---|
| `index.html` | 3 | front-matter `title: "Evidence-Based Dose Constraints"` — the home page's SEO title |
| `index.html` | 4 | front-matter `description: "QUANTEC-inspired radiation dose/constraint resources…"` |
| `index.html` | 98 | "A quick route from an organ at risk to its **constraint** paper" |
| `index.html` | 106 | "Every **constraint** on this site routes to the peer-reviewed review…" |
| `index.html` | 109 | CTA button "Browse **Constraints**" |
| `about.html` | 22 | "Provide up-to-date, evidence-based **dose constraint** recommendations" |
| `about.html` | 39 | "The primary sources behind the **constraints** used in planning…" |

Sweeping these is consistent with the document's intent; not sweeping them is consistent with its
letter. The site's content-authority rule says the owner's words govern, which argues for asking
rather than assuming.

**Ruled: sweep them all.** Implementation then found five more instances the question had not
listed, because Q2 was scoped to `index.html` and `about.html` only:

| File | Line | Was | Now |
|---|---|---|---|
| `quantec.html` | 3 | `title: "Dose Constraints"` | `title: "Toxicity Risk Metrics"` |
| `quantec.html` | 9 | `<h2>QUANTEC Dose Constraints</h2>` | `<h2>QUANTEC Toxicity Risk Metrics</h2>` |
| `quantec.html` | 15 | "the review that carries the constraint" | "the review that carries its metrics" |
| `resources.html` | 10, 24 | redirect stub "Dose Constraints" | "Toxicity Risk Metrics" |
| `contact.html` | 22 | "evidence-based dose constraint resources" | "evidence-based toxicity risk metrics" |

The `quantec.html` pair was not optional once Q1 was ruled: a nav tab reading "Toxicity Risk
Metrics" that lands on a page headed "QUANTEC Dose Constraints" is a worse mismatch than the
button Q2 was originally about.

**Four occurrences were deliberately left alone**, and should not be "finished" by a later pass:

- `publications.html:266` — "they carry no dose constraints, so they are not a source for a
  clinical value". This is a claim about what the vision papers contain, not site branding.
- `publications.html:328` — "tables summarizing dose/volume constraints for all organs",
  describing the linked QUANTEC summary tables. Renaming it would misdescribe a source.
- `quantec.html:463` — "The four limits are a single constraint, met together" sits inside a
  transcribed Notes cell (RTOG 0415). Clinical content; out of bounds.
- `quantec.html:439` — an HTML source comment, not visible copy.

CSS class names (`.constraint-table`, `.constraint-group`, `.constraint-source`,
`.constraint-shared`) are internal identifiers and were not renamed.

### Q3 — Which brain stem spelling?

The owner delegated the choice ("please pick one"). Two words, **"Brain stem"**, was proposed on
the grounds that it matches the transcribed QUANTEC Table 1 source (see
[constraints-pilot-lung-heart-cord.md](constraints-pilot-lung-heart-cord.md), S15) and the
published data tables' caption and ARIA labels.

**Ruled: one word, "Brainstem"** — the recommendation was overruled. Four occurrences changed,
all of them labels or prose rather than transcribed values:

| Line | Was | Now |
|---|---|---|
| `quantec.html:34` | panel note "Brain, brain stem, optic nerve…" | "Brain, brainstem, optic nerve…" |
| `quantec.html:141` | `aria-label="Brain stem dose, volume and outcome data"` | `aria-label="Brainstem dose, volume…"` |
| `quantec.html:143` | `<caption>Brain stem` | `<caption>Brainstem` |
| `quantec.html:161` | `aria-label="Read the Brain stem QUANTEC review, Mayo C"` | `aria-label="Read the Brainstem QUANTEC review…"` |

`quantec.html:30` ("Brainstem injury") and `quantec-2.html:41` ("Brainstem") were already one word
and needed no change, so the ruling reached fewer files than the two-word option would have.

**Two strings stayed fixed, and must stay fixed.** The paper title at `quantec.html:161`,
"Radiation Associated Brainstem Injury", is a citation copied from the source — it happens to be
one word already, but it is immutable regardless of which spelling the prose uses. And the anchor
`id="data-brain-stem"` is unchanged: it is a live public URL fragment, and renaming it would break
any bookmark or deep link pointing at the brainstem table.

## Implementation status

**Implemented 2026-09-03**, in the branch `terminology-edits-2026-08-31`. All 13 mapped items
applied, plus the Q2 sweep (12 further strings across `index.html`, `about.html`, `quantec.html`,
`contact.html` and `resources.html`) — 29 string replacements in total, each applied by exact
match with a uniqueness assertion, so a drifted string would abort the run rather than mis-apply.

**Content accuracy.** No dose value, DVH cut-off, NTCP parameter, paper title, author list,
journal reference or URL was altered. The only edits touching the transcribed constraint tables
were the four brainstem labels under Q3 — a caption and three ARIA labels, none of them a value.

**Release gate, run 2026-09-03:**

| Step | Result |
|---|---|
| 1. `jekyll clean && build` | Exit 0, expected output exactly, no warnings |
| 2. Internal links/anchors | `OK` — 7 pages, 17 served URLs, 85 external skipped |
| 2b. Restated clinical cells | `OK` — 1 restated cell matches its merged cell |
| 3. `_site/` tree | Matches the expected list exactly; no strays |
| 4. Rendered, desktop | `/quantec/` tables clean at 881, 882, 885, 900, 1000, 1204, 1280px |
| 5. Rendered, mobile | Clean at 320, 360, 375, 390, 414, 480, 600, 768, 880px — no wrapper overflow, no clipped text |
| — | The five other pages: no page overflow, no element wider than the viewport, at 320/375/768/1204/1280px |
| 6. Console | Clean |

**One gate detector needs care here.** Release step 5's text-clip recipe compares each text node
against its `<tr>` box, because `.constraint-table tr { overflow: hidden }` is the rule that has
swallowed text before. That comparison produces a **false positive on `<thead>` rows**: the header
row's own `getBoundingClientRect()` collapses to 2px wide while its `<th>` children lay out
normally at 101px, with 74px of text inside them. Run as written, it reports "Technique" clipped
in every table at every width from 320px to 1280px. That is not a clip and not a regression — it
predates this change. Comparing against the *cell* box, falling back to the row only where the
row's rect is the wider of the two, reports clean at every width. Anyone re-running that step
should expect the artifact rather than chase it.
